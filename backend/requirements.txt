fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.6.0
supabase==2.15.3
anthropic==0.39.0
PyJWT[crypto]==2.10.0
cryptography==43.0.3

# PDF parsing — replaces pypdf
# pdfplumber: table detection + text extraction with table-region exclusion
# PyMuPDF:    image extraction (raster + vector page rasterization)
pdfplumber==0.11.9
PyMuPDF==1.27.2.3

# Embedding provider — Google Cloud Gemini Enterprise Agent Platform
# Uses the Google Gen AI SDK (google-genai). The legacy google-cloud-aiplatform
# `vertexai.language_models` SDK is removed by Google after 2026-06-24; this is
# its supported replacement (same model, same aiplatform.googleapis.com endpoint).
# Pinned to the current stable release (verified 2026-06-09).
# Re-pin when upgrading by checking https://pypi.org/project/google-genai/
google-genai==2.8.0
